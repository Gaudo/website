import { GuideSpecification } from 'APP/repository/specifications/GuideSpecification';

class ByIdGuideSpecification implements GuideSpecification {

    public constructor(id : number )
    {
        if
        number = parseint(    
    }

    public specified(guide: Guide) : boolean
    {
        return guide.id === 
    }

}
